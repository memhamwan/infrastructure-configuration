apiVersion: v1
kind: Service
metadata:
  name: prometheus-smokeping
  namespace: default
spec:
  ports:
    - name: http
      port: 9374
      protocol: TCP
      targetPort: 9374
  selector:
    app.kubernetes.io/name: prometheus-smokeping
  sessionAffinity: None
  type: ClusterIP
